- daily_menu.courses.each do |course|
  .col-md-4
    .thumbnail
      = image_tag course.image.url
      .caption
        %p
          %strong
            Name
          = course.name
          %br/
          %strong
            Price:
          = course.price
          %br/
          %strong
            Type:
          = course.course_type